version: 0.2

phases:
  build:
    commands:
      - echo start build at `date`
      - ./gradlew test
      - ls -la build/reports/tests/test
reports:
  junit-sample-report:
    files:
      - '**/*'
    base-directory: build/reports/tests/test
    discard-paths: no
